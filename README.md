```bash
sudo add-apt-repository ppa:neovim-ppa/unstable
sudo apt update
sudo apt install -y neovim
nvim --version
```
```bash
mkdir ~/.config/nvim 
cd ~/.config/nvim
nvim nvim .
nvim nvim
nvim init.lua

:Lazy
```

```bash
git clone https://github.com/EbraamSobhy/Neovim-Config.git ~/.config/nvim
```
